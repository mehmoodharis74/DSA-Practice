def pattern(n):
    ascii = 65
    
    for i in range(n):
        temp = ascii
        for j in range(i + 1 ):
            print (str(chr(temp)), end=" ")
            temp = temp + 1
        print()
            
pattern(7)